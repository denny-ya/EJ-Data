# Main Screen UI Modification Plan

## Goal
Update the Main Screen UI to match the user's provided design references:
1.  **Background**: Apply the design from the "Left Photo" (blue header, clean background).
2.  **Header**: Replace the notification/profile icon with a **Home Icon**.
3.  **Menu List**: Transform the 2-column grid into a 1-column list with a specific card design (from "Right Photo" - horizontal cards with a blue left border).

## Proposed Changes

### Mobile App (`mobile-app`)

#### [MODIFY] [index.tsx](file:///c:/Users/전은진/Desktop/EUNJIN-Project/Create%20App%20Project/mobile-app/app/(tabs)/index.tsx)
-   **Header**:
    -   Replace the `profileIcon` view with an `IconSymbol` (Home icon).
-   **Menu List**:
    -   Change `FlatList` to single column (`numColumns={1}`).
    -   Remove `columnWrapperStyle` (not needed for single column).
    -   Adjust item container styles if necessary.

#### [MODIFY] [MenuCard.tsx](file:///c:/Users/전은진/Desktop/EUNJIN-Project/Create%20App%20Project/mobile-app/components/MenuCard.tsx)
-   **Layout**: Change from `flexDirection: 'column'` (vertical) to `row` (horizontal).
-   **Styling**:
    -   Remove `aspectRatio: 1` constraint.
    -   Set width to fill the available space.
    -   Add a **Blue Vertical Bar** on the left side of the card.
    -   Add a **Chevron/Arrow Icon** (`chevron.right`) on the right side.
    -   Adjust icon and text placement to align horizontally.

#### [MODIFY] [icon-symbol.tsx](file:///c:/Users/전은진/Desktop/EUNJIN-Project/Create%20App%20Project/mobile-app/components/ui/icon-symbol.tsx)
-   Ensure `house.fill` (or similar home icon) and `chevron.right` are available in the icon mapping.

## Verification Plan

### Manual Verification
1.  **Start the App**: Run `npx expo start` to launch the app.
2.  **Check Header**:
    -   Verify the top right icon is a **Home Icon**.
    -   Verify the header background color matches the design (Blue `#0051A2`).
3.  **Check Menu List**:
    -   Verify the menu items are in a **single vertical list**.
    -   Verify the **Menu Card Design**:
        -   Is there a blue vertical bar on the left?
        -   Is the icon on the left?
        -   Is the text in the center?
        -   Is there an arrow icon on the right?
