# Main Screen UI Modification Walkthrough

I have updated the Main Screen UI to match the provided design references.

## Changes Overview

### 1. Header Updates
-   **Background**: Retained the blue header style matching the "Left Photo".
-   **Icon**: Replaced the profile/notification placeholder with a **Home Icon** (`house.fill`).

### 2. Menu List Design
-   **Structure**: Converted the 2-column grid to a **single-column vertical list**.
-   **Card Style**: Implemented the "Right Photo" design:
    -   Horizontal layout.
    -   **Blue Vertical Bar** on the left.
    -   Icon on the left, Text in the center, Chevron (`chevron.right`) on the right.
    -   Clean white/grey look with shadow elevation.

## Verification

### Manual Verification Steps
1.  Run the app using `npx expo start`.
2.  Navigate to the Home Screen.
3.  **Check Header**: Confirm the top-right icon is a Home icon.
4.  **Check Menu**: Confirm the menu items are listed vertically and match the new card design with the blue left border.

## Modified Files
-   `mobile-app/app/(tabs)/index.tsx`: Updated header and list structure.
-   `mobile-app/components/MenuCard.tsx`: Updated card component design.
